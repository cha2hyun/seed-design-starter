# Optional. Enables the `seed-figma` MCP server declared in .cursor/mcp.json.
# Create a Figma personal access token at https://www.figma.com/developers/api#access-tokens
# The `seed-docs` MCP server needs no credentials and works out of the box.
FIGMA_PERSONAL_ACCESS_TOKEN=
