# Cursor skills

`seed-design/` is a symlink to `../../.agents/skills/seed-design`, the canonical vendored copy.
Edit or refresh only `.agents/skills/seed-design/`, then the Cursor path stays in sync.

```bash
pnpm dlx skills add https://github.com/daangn/seed-design --skill seed-design
```
