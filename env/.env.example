# Copy to env/.env.local (gitignored) for machine-specific public build values.
# Every VITE_* value is exposed to the browser. Never put secrets in this file.

# Public API base URL used by the app (optional until a backend is wired).
VITE_API_BASE_URL=
